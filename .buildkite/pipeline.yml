steps:
  - label: "Bazel Tests"
    command: bazel test --test_output=errors //Tests/...
  - label: "Danger"
    commands:
      - bundle install
      - bundle exec danger --verbose
  - label: "Analyze"
    command: make analyze
