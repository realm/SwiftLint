package extension Configuration {
    /// Global configuration values made available to rules during linting.
    var globalConfiguration: GlobalConfiguration {
        GlobalConfiguration(indentation: indentation)
    }
}
